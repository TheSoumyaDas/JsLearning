console.log("Hello Guys!");

const accountId = "sam123";
let accountName = " Soumya";
var userCity = "Mumbai"; // so when we write the var outside the func it can be used anywhere inside the func and outside the func, but when it is used inside the func, it can't be used outside the func!

console.table([accountId, accountName, userCity]);

/*
    Perfer not to use var beacuse of issue in block scope and functional scope
*/
